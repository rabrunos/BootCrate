# Bugfix Task Template

Start from `base.md` and add only what is material:
- observed behavior;
- expected behavior;
- reproduction/conditions;
- affected version/environment when known;
- relevant compact diagnostics/regression evidence;
- root-cause expectation;
- regression/compatibility validation.

If the cause is not sufficiently understood, use an Investigation contract rather than guessing a fix.
