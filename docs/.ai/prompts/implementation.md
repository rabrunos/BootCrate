# Implementation Task Template

Start from `base.md` and use the smallest sufficient shape.

For new behavior, add only what is material:
- desired capability/behavior;
- verified integration points;
- compatibility/migration constraints;
- measurable acceptance criteria;
- required docs/config changes;
- explicit non-goals.

Prefer a narrow validated vertical slice over speculative adjacent work.
