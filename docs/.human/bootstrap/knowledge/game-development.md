# Game Development Heuristics

Discovery should establish engine/custom-engine direction, target platforms, rendering/audio/input/assets, persistence, validation boundaries, packaging/distribution, and networking if applicable.

When using an engine, prefer native lifecycle/serialization/scene/tooling patterns.
When building an engine, separate reusable engine systems from game-specific rules and advance in small validated slices.
