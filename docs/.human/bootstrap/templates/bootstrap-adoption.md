# [<TARGET_VERSION>] Adopt BootCrate into an Existing Project

Relevant Issue: <ISSUE_URL>
Original: <REPOSITORY_BRANCH_SHA_AND_LOCAL_STATE>
Adoption Sandbox: <SEPARATE_CLONE_OR_APPROVED_FORK/WORKTREE>
Owner-approved scope and delta: <ADDITIONS_ADAPTATIONS_REPLACEMENTS_RETIREMENTS>
Selected executors/preset/Console/distribution: <EXACT_CHOICES>
Authorized operations: <PREPARE_ONLY|APPLY_EXACT_HEAD|COMMIT/PUSH_ISSUE_ACTIONS_AS_SPECIFIED>

1. Record baseline identity, native version, product build/test/run/validate commands, useful instructions/automation, CI, data/resource boundaries and known failures. Preserve uncommitted work intentionally or stop. The original stays untouched during the experiment.
2. Inventory old method surfaces and consumers as `preserve`, `adapt`, `replace`, `add`, `retire` or `conflict`. Promote useful product knowledge and behavior before removing conflicts. Compare existing publication immutability and destination records with the new contract; do not weaken protection silently.
3. Prepare an isolated clone using the adoption helper or an equivalent verified mechanism. Complete the **entire selected method** there: Issue workflow, profile, version/changelog, selected executors/skills/preset, security, native checks, optional Console and real destinations only if selected. Do not copy the full bootstrap into the finished project.
4. Compare baseline and sandbox on applicable product behavior, commands, version, CI, instructions, out-of-scope files and local resources. Use the retained structural verifier and native checks. Report blocked/manual/unknown evidence without a false pass.
5. After approval of the exact sandbox HEAD/delta, recheck original HEAD and local state. Apply only the approved delta, including retirement of conflicting old instructions. Repeat applicable checks **in the original**, review `git diff`, version and Issue evidence, and carry out only authorized GitHub/Git actions. A passed sandbox never substitutes for this last check.

If applying fails, preserve sanitized diagnostics and stop. Do not reset the original, copy the entire sandbox over it or close the Issue before required smoke and owner acceptance.
