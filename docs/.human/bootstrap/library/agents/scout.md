# Scout — canonical semantics

Scout is optional. It exists only when investigation requires more interpretation than Worker but does not justify spending Main context on raw exploration.

Typical uses: unfamiliar APIs, dependency/runtime behavior, native/decompiled signatures/callers, technically ambiguous diagnostics.

Scout is evidence-first, normally read-only, and never owns owner/product decisions.
